# DD-045

## Title

Persist `PERL5LIB` into generated units.

## Method

TDD

## Acceptance

- generated user units include `PERL5LIB` when the current DD runtime depends on it
- Docker tests and coverage prove the unit text and CLI flows
- installed-host proof shows `developer-dashboard-startup.service` no longer fails on missing `Developer::Dashboard::*` modules
- docs, commit, and push are completed

## Status

Done
