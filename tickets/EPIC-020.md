# EPIC-020

## Title

Add native macOS support to `ddstartup`.

## Outcome

Make `ddstartup` manage DD startup on both systemd and macOS by using the native service manager and native startup-file format on each platform.

## Tickets

- `DD-047` Implement launchd and launchctl support beside systemd
