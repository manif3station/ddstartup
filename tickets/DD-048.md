# DD-048

## Title

Derive Perl library paths from the installed dashboard layout.

## Method

TDD

## Acceptance

- generated systemd units and macOS plists include dashboard-adjacent Perl library paths such as sibling `lib/perl5` directories when they exist
- `ddstartup` keeps existing runtime `PERL5LIB` or `@INC` entries after the dashboard-derived paths
- macOS launchd startup for `~/perl5/bin/dashboard` style installs no longer fails on missing `Developer::Dashboard::*` modules
- Docker tests, 100% coverage, docs, commit, and push are completed

## Status

Done
