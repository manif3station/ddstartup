# DD-046

## Title

Skip install-time auto-setup on unsupported hosts.

## Method

TDD

## Acceptance

- `make install` exits cleanly when `systemctl` is missing or unusable
- install-time auto-setup returns a skipped result with a clear reason instead of aborting the install
- explicit `dashboard ddstartup.setup` and `dashboard ddstartup.enable` remain strict and still fail clearly when `systemctl` is unavailable
- Docker tests, coverage, docs, commit, and push are completed

## Status

Done
