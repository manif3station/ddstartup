# DD-044

## Title

Add table-default and json-optional output modes.

## Method

TDD

## Acceptance

- `dashboard ddstartup.setup`, `enable`, `disable`, `status`, and `remove` print table output by default
- `dashboard ddstartup.logs` prints table output by default and supports `-o json`
- `-o json` returns JSON for every `ddstartup` command
- Docker tests, coverage, docs, commit, and push are completed

## Status

Done
