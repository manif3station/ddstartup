# EPIC-016

## Title

Create the `ddstartup` startup manager skill.

## Outcome

Deliver a systemd-focused startup manager for DD that works at user scope and root-managed system scope.

## Tickets

- `DD-043` Build `ddstartup` for systemd startup management
