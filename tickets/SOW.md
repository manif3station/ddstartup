# SOW-007

## Title

Create the `ddstartup` skill.

## Objective

Build an isolated DD skill that manages systemd startup for Developer Dashboard by installing a unit that runs `dashboard restart`.

## Deliverables

- isolated `ddstartup` skill repository
- systemd setup, status, logs, and removal commands
- Docker-based tests with 100% coverage
- skill-local docs and ticket records
- release commit and push
