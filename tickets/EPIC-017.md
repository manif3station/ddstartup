# EPIC-017

## Title

Add default table output to `ddstartup`.

## Outcome

Make `ddstartup` commands human-friendly by default while preserving JSON output for scripts.

## Tickets

- `DD-044` Add table-default and json-optional output modes
